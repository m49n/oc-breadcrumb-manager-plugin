# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: dubk0ff.breadcrumbmanager::lang.models.template.id
        searchable: true

    title:
        label: dubk0ff.breadcrumbmanager::lang.models.template.title
        type: text
        searchable: true

    is_active:
        label: dubk0ff.breadcrumbmanager::lang.models.template.is_active
        type: switch

    deleted_at:
        label: dubk0ff.breadcrumbmanager::lang.models.deleted_at
        default: —
        invisible: true

    created_at:
        label: dubk0ff.breadcrumbmanager::lang.models.created_at
        invisible: true

    updated_at:
        label: dubk0ff.breadcrumbmanager::lang.models.updated_at